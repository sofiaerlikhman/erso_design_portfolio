// vite-env.d.ts: A tiny standard file (not something you need to edit)
// that tells TypeScript about extra features Vite adds, like being able
// to import an image file directly and get back its URL, or reading
// import.meta.env.BASE_URL (used in src/lib/withBase.ts).
/// <reference types="vite/client" />
